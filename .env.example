# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=todo
DB_USER=todo
DB_PASSWORD=changeme

# Backend
PORT=3000
LOG_LEVEL=info

# GHCR username (your GitHub username, used in production compose)
GHCR_USERNAME=your_github_username

# GitHub Actions secret (set in repo Settings → Secrets, not in .env)
# GHCR_TOKEN: PAT with write:packages scope
